import 'package:flutter/material.dart';

class DoctorSetupPage extends StatelessWidget {
  const DoctorSetupPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: SizedBox.shrink());
  }
}
